# Agent Team 角色定义

> 本文件定义 Agent Team 中各角色的职责、约束和协作规则。
> 开发工作流和分级规则见 `CLAUDE.md` 的"开发工作流"章节。

<!-- CUSTOMIZE: 根据你的技术栈替换以下角色的技术栈、关键文件路径、启停命令 -->

## 角色

### implementer-backend

**职责：** 后端功能开发 — API 端点、数据库模型、服务层。

<!-- CUSTOMIZE: 替换为你的后端技术栈 -->

| 维度 | 说明 |
|------|------|
| 技术栈 | <your-backend-tech>（如 Python FastAPI + SQLAlchemy） |
| 关键文件 | <your-backend-src>/（如 `src/backend/routers/`、`src/backend/services/`） |
| 测试要求 | 新 API 端点必须有对应集成测试 |
| 启动/测试 | <your-backend-start-cmd> / <your-test-cmd> |

**约束：**
- 不修改前端代码
- 数据结构强类型，使用语言对应的类型系统
- 遵循项目的包管理和虚拟环境规范

### implementer-frontend

**职责：** 前端功能开发 — 页面、组件、API 调用、样式。

<!-- CUSTOMIZE: 替换为你的前端技术栈 -->

| 维度 | 说明 |
|------|------|
| 技术栈 | <your-frontend-tech>（如 Next.js + React + Tailwind） |
| 关键文件 | <your-frontend-src>/（如 `src/frontend/src/app/`、`src/frontend/src/components/`） |
| 测试要求 | 页面级 E2E 测试覆盖核心交互路径 |
| 启动/测试 | <your-frontend-start-cmd> / <your-e2e-test-cmd> |

**约束：**
- 不修改后端代码
- TypeScript 严格类型（如适用）
- 遵循项目的包管理规范

### test-writer

**职责：** 测试编写与运行 — 单元测试、集成测试、E2E 测试。

<!-- CUSTOMIZE: 替换为你的测试框架 -->

| 维度 | 说明 |
|------|------|
| 技术栈 | <your-test-framework>（如 pytest + Playwright） |
| 关键文件 | <your-test-dir>/（如 `tests/unit/`、`tests/integration/`、`tests/e2e/`） |
| 测试要求 | 覆盖所有新增 API 端点和前端页面核心交互 |
| 启动/测试 | <your-test-cmd> |

**约束：**
- 不修改业务代码，除非为了让测试通过需要修复 bug
- 测试文件结构镜像源码目录结构
- 集成测试使用测试数据库，不污染生产数据

## 协作规则

| 规则 | 说明 |
|------|------|
| Feature Branch | 所有 Agent 在同一 feature branch 上工作 |
| 分支命名 | `feat/<topic>` / `fix/<topic>` / `refactor/<topic>` |
| Commit 规范 | Conventional Commits：`feat:` / `fix:` / `test:` / `docs:` / `refactor:` |
| PR 体量 | <= 500 LOC，超过时拆分 |
| 测试覆盖 | 新 API 端点必须有集成测试，新页面必须有 E2E 测试 |
| 文件冲突 | 两个 Agent 需修改同一文件时，通过 task 依赖（`addBlockedBy`）串行化 |
| 沟通方式 | Agent 之间通过 SendMessage 沟通，不通过文件传递信息 |
| 进度更新 | 每完成一个 task 更新 `task_plan.md` checkbox + `progress.md` 状态 |
